name: Deploy ELA Navigator

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy
        run: |
          cd /home/adminpaco/elanavigator-guide
          ./deploy.sh
